[package]
name = "test-setup"
version = "0.1.0"
authors = ["Tom Houlé <tom@tomhoule.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
url = "2.2.0"
tracing-subscriber = { version = "0.2.15", features = ["fmt"] }
tokio = { version = "0.2.23", optional = true }
once_cell = "1.5.2"
enumflags2 = "0.6.4"
tracing-error = "0.1.2"
connection-string = "0.1.10"

[dependencies.quaint]
git = "https://github.com/prisma/quaint"
optional = true
features = [
    "postgresql",
    "mysql",
    "mssql",
    "sqlite",
    "json",
    "uuid",
    "chrono",
    "bigdecimal",
]

[features]
default = ["sql"]
sql = ["quaint", "tokio"]
