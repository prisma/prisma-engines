[package]
name = "sql-ddl"
version = "0.1.0"

[features]
postgres = []
mysql = []
sqlite = []
mssql = []
default = ["postgres", "mysql", "sqlite", "mssql"]

[dependencies]

[dev-dependencies]
indoc = "1.0"
