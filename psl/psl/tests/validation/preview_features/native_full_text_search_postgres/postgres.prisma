generator client {
  provider        = "prisma-client"
  previewFeatures = ["fullTextSearchPostgres"]
}

datasource db {
  provider = "postgres"
}

model Blog {
  id      Int    @unique
  content String
  title   String
  @@index([content, title])
}