generator client {
  provider        = "prisma-client"
  previewFeatures = ["schemaEngineDriverAdapters"]
}

datasource db {
  provider = "sqlite"
}

model Blog {
  id      Int    @unique
  content String
  title   String
}
