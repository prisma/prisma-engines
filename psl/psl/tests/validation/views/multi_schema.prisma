datasource db {
  provider = "postgresql"
  schemas  = ["A", "B"]
}

generator js {
  provider = "prisma-client"
  previewFeatures = ["views"]
}

model A {
  id Int @id

  @@schema("B")
}

view Mountain {
  id Int

  @@schema("A")
}
