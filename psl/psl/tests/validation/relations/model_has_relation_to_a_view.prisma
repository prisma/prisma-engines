generator js {
  provider        = "prisma-client-js"
  previewFeatures = ["views"]
}

view SomeUser {
  id      Int      @unique
  profile Profile?
}

model Profile {
  id      Int       @id
  user    SomeUser? @relation(fields: [user_id], references: [id])
  user_id Int       @unique
}

