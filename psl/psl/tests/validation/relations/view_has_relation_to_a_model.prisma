generator js {
  provider        = "prisma-client-js"
  previewFeatures = ["views"]
}

view SomeUser {
  id      Int      @unique
  profile Profile? @relation(fields: [id], references: [id])
}

model Profile {
  id   Int       @unique
  user SomeUser?
}

