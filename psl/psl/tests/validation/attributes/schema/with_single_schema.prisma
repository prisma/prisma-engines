datasource testds {
    provider = "postgresql"
    url = env("TEST_DATABASE_URL")
    namespaces = ["public"]
}

generator js {
    provider = "prisma-client-js"
    previewFeatures = ["multiSchema"]
}

model Test {
  id Int @id
  @@namespace("public")
}

enum Language {
  English
  Spanish

  @@namespace("public")
}


