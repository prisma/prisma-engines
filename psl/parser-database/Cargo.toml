[package]
name = "parser-database"
version = "0.1.0"
edition = "2021"

[dependencies]
diagnostics = { path = "../diagnostics" }
schema-ast = { path = "../schema-ast" }

enumflags2 = "0.7"
indexmap = "1.8.0"
either = "1.6.1"
