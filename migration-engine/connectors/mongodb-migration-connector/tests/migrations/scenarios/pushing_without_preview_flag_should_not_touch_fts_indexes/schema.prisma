 datasource db {
   provider = "mongodb"
   url      = "mongodb://"
 }

 model User {
   id        String @id @map("_id") @db.ObjectId
   age       Int
   firstName String
   lastName  String
   location  String
 }
