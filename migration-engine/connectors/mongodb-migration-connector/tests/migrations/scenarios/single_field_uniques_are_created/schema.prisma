 datasource db {
   provider = "mongodb"
   url      = "mongodb://"
 }

 generator js {
   provider        = "prisma-client-js"
   previewFeatures = ["extendedIndexes", "fullTextIndex", "mongodb"]
 }

model User {
    id Int @id
    name String @unique
}

