generator client {
    provider        = "prisma-client"
    previewFeatures = []
}

datasource db {
    provider     = "postgresql"
    relationMode = "prisma"
}

model A {
    id Int @id

    @@schema("base")
}
