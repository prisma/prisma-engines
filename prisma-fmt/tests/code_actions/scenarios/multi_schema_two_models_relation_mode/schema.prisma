generator client {
    provider        = "prisma-client"
    previewFeatures = []
}

datasource db {
    provider     = "postgresql"
    url          = "dummy-url"
    relationMode = "prisma"
    schemas      = ["one", "two"]
}

model User {
    id     Int  @id
    post   Post @relation(fields: [postId], references: [id])
    postId Int
}

model Post {
    id   Int    @id
    User User[]

    @@schema("two")
}
