generator client {
    provider        = "prisma-client"
    previewFeatures = []
}

datasource db {
    provider = "postgresql"
    url      = env("TEST_DATABASE_URL")
    schemas  = ["one", "two"]
}
