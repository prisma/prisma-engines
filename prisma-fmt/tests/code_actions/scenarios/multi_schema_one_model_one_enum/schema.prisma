generator client {
    provider        = "prisma-client"
    previewFeatures = []
}

datasource db {
    provider = "postgresql"
    url      = "dummy-url"
    schemas  = ["one", "two"]
}

model User {
    id Int @id
}

enum Colour {
    Red
}
