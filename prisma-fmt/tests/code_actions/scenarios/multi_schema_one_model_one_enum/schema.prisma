generator client {
    provider        = "prisma-client"
    previewFeatures = []
}

datasource db {
    provider = "postgresql"
    url      = env("TEST_DATABASE_URL")
    schemas  = ["one", "two"]
}

model User {
    id Int @id
}

enum Colour {
    Red
}
