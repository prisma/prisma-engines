generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["prismaSchemaFolder", "views", "fullTextIndex"]
}

datasource db {
    provider = "mysql"
    url      = env("DATABASE_URL")
}
