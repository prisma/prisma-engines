generator js {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgres"
    url      = env("DATABASE_URL")
}

model ModelNameA {
    id Int @id

    poly Poly
}

/// This is doc for B
enum Po<|>ly {
Am
Nesia
}
