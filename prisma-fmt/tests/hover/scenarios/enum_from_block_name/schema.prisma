generator js {
    provider = "prisma-client"
}

datasource db {
    provider = "postgres"
    url      = env("DATABASE_URL")
}

model ModelNameA {
    id Int @id

    poly Poly
}

/// This is doc for B
enum Po<|>ly {
Am
Nesia
}
