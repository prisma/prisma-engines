generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["views"]
}

datasource d<|>b {
provider = "mongodb"
url      = env("DATABASE_URL")
}

enum Pet {
  Cat
  Dog
  Redpanda
}
