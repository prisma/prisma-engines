generator client {
    provider        = "prisma-client"
}

datasource db {
    provider = "postgres"
}

model Compound {
    id      String
    name    String

    @@unique(fields: [id, n<|>ame])
}

