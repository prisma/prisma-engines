generator js {
  provider = "prisma-client"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

model A {
  id  Int   @id
  val Int[]

  @@index([val(ops: <|>)], type: Gin)
}
