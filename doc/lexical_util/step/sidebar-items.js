window.SIDEBAR_ITEMS = {"fn":[["max_step","Calculate the maximum number of digits that can be processed without always overflowing for a given type. For example, 20 digits can be processed for a decimal string for `u64` without overflowing, but it may overflow."],["min_step","Calculate the maximum number of digits that can always be processed without overflowing for a given type. For example, 19 digits can always be processed for a decimal string for `u64` without overflowing."],["u64_step","Calculate the number of digits that can be processed without overflowing a u64. Helper function since this is used for 128-bit division."]]};