[package]
name = "schema-builder"
version = "0.1.0"

[dependencies]
psl.workspace = true
schema = { path = "../schema" }
prisma-models = { path = "../prisma-models" }
once_cell = "1.3"
itertools = "0.10"
lazy_static = "1.4"
