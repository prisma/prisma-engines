model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [childId], references: [id]) 
 childId String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [childId], references: [id]) 
 childId String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [childId], references: [id]) 
 childId String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [childId], references: [id]) 
 childId String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [childId], references: [id]) 
 childId String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [childId], references: [id]) 
 childId String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [childId], references: [id]) 
 childId String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [childId], references: [id]) 
 childId String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [childId], references: [id]) 
 childId String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [child_id_1, child_id_2], references: [id_1, id_2])
 child_id_1 String?
 child_id_2 String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [child_id_1, child_id_2], references: [id_1, id_2])
 child_id_1 String?
 child_id_2 String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [child_id_1, child_id_2], references: [id_1, id_2])
 child_id_1 String?
 child_id_2 String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [child_id_1, child_id_2], references: [id_1, id_2])
 child_id_1 String?
 child_id_2 String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [child_id_1, child_id_2], references: [id_1, id_2])
 child_id_1 String?
 child_id_2 String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [child_id_1, child_id_2], references: [id_1, id_2])
 child_id_1 String?
 child_id_2 String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [child_id_1, child_id_2], references: [id_1, id_2])
 child_id_1 String?
 child_id_2 String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [child_id_1, child_id_2], references: [id_1, id_2])
 child_id_1 String?
 child_id_2 String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields: [child_id_1, child_id_2], references: [id_1, id_2])
 child_id_1 String?
 child_id_2 String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentId], references: [id]) 
 parentId String?
@@unique([parentId])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields:[parent_c], references: [c])
parent_c String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields:[parent_c], references: [c])
parent_c String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields:[parent_c], references: [c])
parent_c String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields:[parent_c], references: [c])
parent_c String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields:[parent_c], references: [c])
parent_c String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? @relation(fields:[parent_c], references: [c])
parent_c String?
    non_unique    String?
    #id(id, String, @id, @default(cuid()))

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? 

    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parent_id_1, parent_id_2], references: [id_1, id_2]) 
 parent_id_1 String?
 parent_id_2 String?
@@unique([parent_id_1, parent_id_2])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    #id(id, String, @id, @default(cuid()))

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    id_1          String        @default(cuid())

                 id_2          String        @default(cuid())

                 @@id([id_1, id_2])

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


model Parent {
    p             String    @unique
    p_1           String
    p_2           String
    childOpt Child? 
    non_unique    String?
    

    @@unique([p_1, p_2])
}

model Child {
    c              String    @unique
    c_1            String
    c_2            String
    parentOpt Parent? @relation(fields: [parentRef], references: [p]) 
 parentRef String?
@@unique([parentRef])
    non_unique     String?
    

    @@unique([c_1, c_2])
}


