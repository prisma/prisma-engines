 model User {
     id Int @id
     firstName String
     lastName String
     age Int

    @@index([firstName, lastName, age])
 }

 model Post {
     id Int @id
     title String

     @@index([title])
 }

